apiVersion: v1
kind: Secret
metadata:
  labels:
    template: cronjob-ldap-group-sync
  name: {{ .Values.ldap_sync.ldap_bind_password_secret }}
  namespace: {{ .Values.ldap_sync.namespace }}
stringData:
  bind_password: {{ .Values.ldap_sync.ldap_bind_password }}
type: Opaque
