apiVersion: v1
kind: Service
metadata:
  name: {{ include "nmstate-helm-charts.fullname" . }}
  labels:
    {{- include "nmstate-helm-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nmstate-helm-charts.selectorLabels" . | nindent 4 }}
