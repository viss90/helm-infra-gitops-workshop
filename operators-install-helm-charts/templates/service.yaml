apiVersion: v1
kind: Service
metadata:
  name: {{ include "operators-install-helm-charts.fullname" . }}
  labels:
    {{- include "operators-install-helm-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "operators-install-helm-charts.selectorLabels" . | nindent 4 }}
